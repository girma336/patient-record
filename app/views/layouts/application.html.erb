<!DOCTYPE html>
<html>
  <head>
    <title>PatientRecord</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="w-[100%] grid grid-cols-12 relative">
      <div class="fixed z-[1000000 right-0 top-[-10px] col-span-3">
      <%= render "splashs/header", class: "fixed overflow-hidden" %>
      </div>
      <div class="z-[100] pt-4 pl-10 relative col-span-9 w-full h-[100vh] bg-gradient-to-tr from-white via-gray-100 to-white">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
        <%= yield %>
      </div>
    </div>
  </body>
</html>
